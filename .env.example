# =============================================================================
# Abuse-Ring Sentinel — Environment Configuration Template
# =============================================================================
#
# No external secrets, database passwords, or cloud API keys are required
# for the default local evaluation, test suite, or dashboard demo.
#
# All risk models, deterministic explainability rules, and synthetic evaluation
# datasets are fully self-contained within this repository.
#
# Optional overrides for local dashboard deployment:
# HOST=127.0.0.1
# PORT=8000
# LOG_LEVEL=info
# =============================================================================
